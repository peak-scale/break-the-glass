---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: accessrules.addons.projectcapsule.dev
spec:
  group: addons.projectcapsule.dev
  names:
    kind: AccessRule
    listKind: AccessRuleList
    plural: accessrules
    singular: accessrule
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AccessRule is the Schema for the accessrules API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessRuleSpec defines the desired state of AccessRule.
            properties:
              action:
                default: AutoApprove
                description: Action to execute when the AccessRule is matched.
                enum:
                - AutoApprove
                - Deny
                type: string
              allowedRoles:
                items:
                  description: AccessRuleSpec defines the desired state of AccessRule.
                  properties:
                    allowedRoles:
                      items:
                        type: string
                      type: array
                    namespaced:
                      default: true
                      description: Whether this rule applies to the namespace scope
                        of the Access Request or the cluster scope.
                      type: boolean
                  required:
                  - allowedRoles
                  type: object
                type: array
              duration:
                default: 60m
                description: Time AccessRequests are valid for, after which they will
                  be automatically deleted.
                type: string
              keepDuration:
                default: 6h
                description: The Duration for which AccessRequests will be kept in
                  the system after they are expired (auditing reasons).
                type: string
              maxDuration:
                default: 6h
                description: Time AccessRequests are valid for, after which they will
                  be automatically deleted.
                type: string
              subjects:
                description: Subjects is the context from where AccessRequests (or
                  from whom) can be created and assigned to this Rule.
                items:
                  type: string
                type: array
            required:
            - allowedRoles
            - duration
            - keepDuration
            - maxDuration
            - subjects
            type: object
          status:
            description: AccessRuleStatus defines the observed state of AccessRule.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
